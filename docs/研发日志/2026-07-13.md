# 研发日志 · 2026-07-13

## 今日目标
收尾：对话确认卡、浏览器到期提醒、页面体验与 bug 修复，同步文档。

## 完成内容
- 对话识别记账/提醒/余额 → 可编辑确认卡，点确定才写入（不再自动落库）
- 浏览器 Notification 到期弹窗（主路径）；Server酱收进「我的」可选折叠
- 响应式：手机底栏 + 桌面侧栏；管家页大屏双栏
- 「我的」账号中心；底栏「设置」改为「我的」
- Bug/体验收尾：
  - apiFetch 401 统一清 token 并跳转登录
  - 提醒到期比较改为本地 `datetime.now()`（与前端 datetime-local 一致）
  - 确认卡按卡 busy；聊天失败回滚用户气泡并恢复输入
  - 提醒页：逾期/即将到期、删除确认、延后 1 天、待办/已完成分区
  - 设置页切换 AI 提供商不冲掉已填 Base URL / 模型
  - 统计默认 Tab 为概览；已登录访问登录/注册页自动回首页

## 问题与处理
- 微信推送路径：WxPusher → Server酱 → 用户反馈不便 → 改为浏览器弹窗为主
- 提醒逾期误判：后端曾用 `utcnow` 与本地墙钟比较，已改为 `now()`

## 明日 / 考核待办
- [ ] 线上 URL 部署与演示
- [ ] 个人总结、录屏、AI Code Review 截图
- [ ] 确认 README / prompt_log / 跨 ≥3 天提交记录齐全
