# 云服务器 venv 部署用（复制为 deploy/.env 后修改）
# cp deploy/.env.example deploy/.env

# 公网 IP 或域名（前端构建时写入，浏览器用来调后端）
PUBLIC_HOST=127.0.0.1

BACKEND_PORT=5000
FRONTEND_PORT=3000

# 后端 Python：默认 backend/.venv；若已有虚拟环境可改成绝对路径
# 例：VENV_DIR=/home/ubuntu/.venv-vita
VENV_DIR=
